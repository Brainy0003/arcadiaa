{{#if hasMessages }}
  <div class="alert alert-success alert-dismissible" role="alert">
    <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
    {{#each messages}}
      <p class="text-center">
        {{this}}
      </p>
    {{/each}}
  </div>
{{/if}}

<h1 class="text-center">Salut, {{ username }}.</h1>
<div class="row">
  <div class="col-sm-6">
    <h2>Bienvenue sur ton profil.</h2>
    <h4>Il est incomplet à l'heure d'aujourd'hui car Supercell n'autorise toujours pas l'accès à leurs données.</j4>
    <h4>Mais dès que cela sera possible, tu pourras voir tes stats ici!</h4>
  </div>
  <div class="col-sm-6">
    <div class="row">
      <div class="col-sm-12">
        <h2>Mes sondages</h2>
        <ul class="list-group">
          {{#each polls}}
            <a class="list-group-item" href="/poll/{{this.id}}">{{this.title}}</a>
          {{/each}}
        </ul>
      </div>
      <div class="col-sm-12">
        <h2>Paramètres</h2>
        <button type="button" class="btn btn-danger" data-toggle="modal" data-target="#deleteAccount">
          Supprimer mon compte
        </button>
      </div>
    </div>
  </div>
</div>

<!-- Delete account -->
<div class="modal fade" id="deleteAccount" role="dialog">
  <div class="modal-dialog" role="document">
    <div class="modal-content panel-danger">
      <div class="modal-header panel-heading">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
        <h4 class="modal-title" id="myModalLabel"><i class="fa fa-2x fa-exclamation-circle" aria-hidden="true"></i></h4>
      </div>
      <div class="modal-body">
        <ul>
          <li>Cette étape est <strong>irréversible.</strong></li>
          <li>Vous perdrez votre statut de chef si vous en êtes un.</li>
          <li>Vous pouvez vous réinscrire à tout moment.</li>
        </ul>
      </div>
      <div class="modal-footer">
        <form action="/user/delete" method="delete">
          <div class="btn-group btn-group-justified" role="group">
            <div class="btn-group" role="group">
              <button type="button" class="btn btn-success" data-dismiss="modal">Tout compte fait...</button>
            </div>
            <div class="btn-group" role="grid">
              <button type="submit" class="btn btn-danger" id="buttonDeleteAccount">Je veux supprimer mon compte!</button>
            </div>
          </div>
        </form>
      </div>
    </div>
  </div>
</div>
